export interface D {
  bundleId: number
  bundle?: any
}

export interface M {}

export interface C {
  hasBundleAvailable: boolean
  noBundle: string
}

export interface P {
  numberOfElements: number
  totalBundle: number
  connection: string
}
